name = "insportia"
main = "src/index.js"
compatibility_date = "2026-08-05"

[assets]
directory = "./public"
binding = "ASSETS"
# Serve index.html for the root and let the Worker handle /api/* routes;
# every other request falls through to the static files in ./public.
not_found_handling = "404-page"
